# DataMind（数据灵析）前端页面设计稿（TypeScript）

## 1. 设计原则
- 简约清晰：信息分区明确，主操作突出，次要信息收起。
- 一致性：统一色板、组件尺寸、留白节奏，采用 12 列栅格。
- 低干扰：默认浅色主题，支持暗色；重要状态用色块或标签标示。
- 可扩展：页面/卡片模块化，数据源、模型、图表类型可插拔。
- 用户友好：清晰的空状态、加载/错误提示，关键路径 ≤ 3 步完成查询出图。

## 2. 信息架构与页面
- 顶部导航：Logo + 产品名 + 全局搜索（模板/数据源/图表）+ 用户/设置/主题切换。
- 侧边导航：工作台、模板管理、数据源管理、模型网关、日志与观测、设置。

### 2.1 工作台（Home）
- 卡片区：最近查询、常用模板、公告与升级提示。
- 快捷入口：新建查询、导入模板、查看日志。

### 2.2 查询工作台（核心页）
- 左侧（参数与意图）
  - 自然语言输入框（多行，带提示与示例）。
  - 模板选择下拉；数据源选择（MySQL/PostgreSQL/ChromaDB/Milvus）。
  - 模型场景选择：意图/SQL/向量/总结，可绑定默认模型（阿里云/豆包/Kimi/OpenAI）。
  - 参数面板：时间范围、维度/指标选择、过滤条件（行内标签 + 弹窗筛选）。
- 中间（链路与生成）
  - 生成的 SQL/向量请求展示区（可折叠），支持复制与重新生成。
  - 执行按钮 + 运行进度/耗时提示；重试/取消。
- 右侧（结果与可视化）
  - 结果标签页：图表 / 数据表 / 日志。
  - 图表区：主图（饼/柱/折/面积/散点/表格/组合），顶部下拉可切换图型；悬浮 tooltip。
  - 交互：筛选（点击/框选）、缩放（时间轴）、导出 PNG/CSV。
  - 空状态与错误状态：给出可操作建议（调整时间/减少字段/换模型）。

### 2.3 模板管理
- 列表：名称、数据源、默认图型、更新时间、发布状态、创建人。
- 行内操作：预览、编辑、发布/下线、复制、版本记录。
- 编辑抽屉：自然语言示例、参数定义（类型/必填/默认）、默认模型、默认图型、权限范围。

### 2.4 数据源管理
- 列表：名称、类型、状态、更新时间、创建人。
- 新建/编辑表单：类型、连接信息、认证方式、数据库/collection、超时、最大行数。
- 操作：连通性测试、仅读凭据提示、Schema 同步（显示最新时间）。

### 2.5 模型网关
- 卡片：阿里云/豆包/Kimi/OpenAI，显示健康度、成功率、P95。
- 表格：模型名、用途(intent/sql/summary/embedding)、QPS/日配额、延迟、成功率、状态。
- 路由策略配置：场景 → 主模型 + 备选列表；规则（延迟/成本/成功率优先）；熔断/降级开关。
- 凭据管理：密文占位，支持轮换；调用日志快捷入口。

### 2.6 日志与观测
- 标签页：查询链路日志 / 模型调用日志 / 告警。
- 查询日志列：时间、用户、数据源、模型、耗时、状态、trace_id（可复制），点击展开查看 SQL/向量与错误。
- 模型日志列：模型、scenario、latency、tokens、cost、status、error_type。
- 筛选：时间、用户、数据源、模型、状态；导出 CSV。

### 2.7 设置
- 主题（亮/暗）、语言（中/英预留）、个人 API Token、团队权限入口。

## 3. 全局组件规范
- 表单：标签左对齐，必填标*，行内校验；支持折叠高级选项。
- 按钮：主按钮用于执行/保存；次按钮用于预览/重置；危险操作用警示色。
- 提示：气泡/警示条用于告警与安全提示（仅读账号、配额、熔断）。
- 空状态：插画 + 文案 + 行动按钮（新建/重新生成/调整过滤）。
- Loading/Error：局部骨架屏；错误卡片支持复制错误与重试。
- 图表组件：统一数据协议 {data, fields(meta: type/role), recommendedCharts, interactions}。
- 栅格：12 列，左右留白 24px，组件间距 16px。

## 4. 交互与状态
- 查询执行态：空闲 / 生成中 / 执行中 / 成功 / 失败；顶部进度条 + 右上运行耗时。
- 回退与历史：保留最近 10 次生成的 SQL/向量（右侧“历史”抽屉），可回溯。
- 并发与限流提示：在模型或数据源达到配额时弹提示并给出替代方案（切换模型/减小时间范围）。
- 可访问性：键盘可导航，按钮有 focus 态，颜色对比度满足 WCAG AA。

## 5. 响应式与布局
- ≥1440px：三栏（参数/链路/结果），侧边栏展开。
- 1024-1439px：两栏（参数/结果），链路面板折叠为抽屉。
- <1024px：侧边导航收起为抽屉，结果区全宽；复杂操作提示在桌面端完成。

## 6. 主题与样式
- 色板：浅色为主，强调色用于主按钮/选中态；暗色反相并降低饱和度。
- 字体层级：标题 18/16，正文 14，辅助 12；行高 1.5。
- 圆角与阴影：卡片圆角 8，阴影轻微；表单控件圆角 6。

## 7. 可扩展性
- 图表注册机制：支持新增图表类型（漏斗/雷达/桑基）通过统一接口挂载。
- 数据源/模型插件：前端表单与后端 schema 解耦，通过元数据驱动渲染字段。
- 行为埋点：关键操作（生成/执行/导出/发布）发送埋点，支持 A/B。

## 8. 关键交互流
1) 快速查询：选择模板 → 填参 → 生成并执行 → 查看图表 → 导出。
2) NL 自助：输入自然语言 → 选择数据源/模型 → 查看生成 SQL/向量 → 执行 → 切换图型。
3) 模型路由调整：在模型网关选择场景 → 设定主/备模型 → 保存 → 查询链路自动生效。
4) 数据源新增：填写表单 → 测试连通 → 保存 → Schema 同步成功提示。
5) 故障自愈：执行失败 → 错误提示 + 建议（缩小范围/换模型/降级为表格）→ 一键重试。

## 9. 接口对接要点（前端视角）
- 配置拉取：数据源列表、模型注册与路由、模板列表、图表推荐接口。
- 查询执行：`/query/intent`（意图解析+模型路由）→ `/query/execute`（SQL/向量执行）。
- 日志：分页查询链路日志与模型调用日志；导出接口。
- 权限：获取用户角色/团队与可见资源列表；敏感操作二次确认。

## 10. 性能与体验优化
- 懒加载：图表库与重型组件按需加载；日志列表虚拟滚动。
- 缓存：模板与数据源元数据本地缓存；最近查询保存在 session。
- 防抖/节流：自然语言输入提示、全局搜索建议。
- 失败兜底：超时提示与降级（仅表格输出/截断行数）。

