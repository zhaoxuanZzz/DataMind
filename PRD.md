# DataMind（数据灵析）PRD

## 1. 产品概述
- 定位：智能数据分析与可视化工具，面向运营/分析/业务团队，支持自然语言与可视化配置完成多源查询、分析与图表呈现。
- 核心卖点：智能意图识别、多数据源/向量库适配、自动图表推荐、低代码页面配置，Python 后端以 LangChain+LangGraph 编排确保可扩展。

## 2. 目标与成功标准
- 目标
  - 提升业务自助分析效率，降低对数据/开发团队依赖。
  - 统一关系型与向量数据的查询与可视化入口。
  - 提供可配置的多图表呈现与洞察建议。
- 成功标准（MVP）
  - 80% 的常见查询可用自然语言完成，无需手写 SQL。
  - 支持 MySQL、PostgreSQL、ChromaDB；后续扩展 Milvus。
  - 单页可视化图表支持饼/柱/折线图切换；响应时间 < 5s（P95，10k 行以内）。
  - 关键操作有审计日志，错误率 < 2%（非超时类）。

## 3. 用户与场景
- 用户画像：运营/分析师、产品/业务经理、数据开发（配置数据源）、管理员（权限、审计）。
- 主要场景
  - 运营报表：分渠道/地区/时间的指标对比与趋势。
  - 实验分析：分组对照的指标、Top-N、漏斗、留存。
  - 相似度检索：基于向量库的内容/商品/FAQ 召回与排序。
  - 自助问答：自然语言提问，自动生成 SQL 或向量检索并出图。

## 4. 核心功能需求
- 数据源管理
  - 配置 MySQL/PostgreSQL/ChromaDB/Milvus，表单校验，连通性测试。
  - 管理凭据（加密存储），支持 token 方式下发。
  - Schema/collection 元数据同步与缓存。
- 查询与意图解析
  - 自然语言输入，识别指标/维度/时间范围/过滤条件/预期图表类型。
  - 模板化查询（参数化），支持下拉/日期/多选等控件。
- SQL/向量生成与执行
  - 基于 schema 生成安全 SQL（限制 DDL/DML，自动 limit/timeout）。
  - 向量检索：支持 topK、相似度度量、过滤条件。
  - 混合查询：关系型结果与向量结果的合并/排序。
- 可视化与交互
  - 图表类型：饼/环/柱/条/折/面积/散点/表格/组合图。
  - 推荐逻辑：按指标/维度类型与数据量选择默认图型，可手动切换。
  - 交互：过滤、联动高亮、区间缩放、tooltip、导出 CSV/PNG。
- 模型管理（LLM 网关）
  - 适配多家模型：阿里云、豆包、Kimi、OpenAI。
  - 模型注册与分组：按场景（SQL 生成/意图识别/向量增强/总结）配置默认与候选模型。
  - 路由策略：基于延迟、成本、成功率的优选；失败自动降级/重试切换。
  - 额度与并发：QPS 限流、每日调用配额、按 key 维度的使用统计。
  - 凭据管理：密文存储，前端不回显；可分发 token。
  - 观测与告警：请求日志、耗时、错误类型，失败率阈值告警。
- 配置与发布
  - 页面/卡片级配置：数据源绑定、查询模板、默认图型、权限。
  - 版本管理：保存/回滚；发布记录。
  - 权限：按角色/团队可见，操作审计。
- 可观测性与安全
  - 查询链路日志：输入、生成 SQL/向量、执行耗时、错误。
  - 重试与熔断策略；超时配置。
  - 防提示注入、参数化查询、最小权限数据库账号。

## 5. 非功能需求
- 性能：P95 响应 < 5s（10k 行以内）；并行可拆分节点；缓存热门 schema/查询。
- 可靠性：节点级重试，执行失败可降级（返回部分数据或推荐过滤）。
- 安全：加密存储凭据；操作审计；权限分级；限制高危 SQL。
- 可用性：前端错误提示友好，可提供重试/反馈。

## 6. 关键用户故事
- 作为运营，我在查询面板输入“近30天各渠道日活及环比”，系统生成 SQL、执行并默认以折线+柱状组合展示，可切换为表格导出。
- 作为分析师，我为 A/B 实验创建模板，配置分组/时间/指标参数，发布给团队，成员可填写参数获得漏斗或折线对比。
- 作为产品经理，我输入“与新品 X 相似的前 20 个商品”，系统走向量检索，返回列表并提供相似度得分与可视化分布。
- 作为数据开发，我新增 Milvus 数据源，测试连接，保存后团队可在查询中选择该源。
- 作为管理员，我可查看查询审计日志、失败原因、节点耗时，并对高危操作进行告警。

## 7. 用户流程（简要）
1) 登录/鉴权 → 进入查询页面或模板页
2) 选择数据源/模板 → 输入自然语言或填参
3) 提交 → LangGraph 编排：意图解析 → 模型路由（选择阿里云/豆包/Kimi/OpenAI 等）→ 数据源选择 → SQL/向量生成 → 执行 → 可视化推荐
4) 前端渲染结果 → 用户交互（切换图型/过滤/导出）
5) 日志与审计记录 → 可观测面板可查看

## 8. 交互与前端要求（TypeScript）
- UI 组件：数据源配置表单、查询输入区、参数面板、图表组件库、日志/调试面板。
- 状态管理：查询状态（进行中/成功/失败）、结果缓存、图表配置。
- 图表接口：统一数据+meta 协议（字段类型、指标/维度映射、推荐图型）。
- 可扩展性：支持新增图表类型与数据源选择器。
- 模型网关配置前端：支持为不同场景选择默认模型、设定 QPS/日配额、查看健康状态与近 24h 成功率。

## 9. 后端与编排要求（Python + LangChain + LangGraph）
- 节点划分：意图解析、数据源选择、SQL 生成、向量检索、执行与聚合、可视化推荐、错误处理/重试。
- 工具接口：SQL 生成器、Schema 读取器、向量检索器、指标计算器。
- 模型网关：统一封装阿里云/豆包/Kimi/OpenAI API；支持路由策略（成本/延迟/成功率）、健康检查、熔断与降级。
- 安全约束：SQL 沙箱、limit/timeout、白名单函数，记录生成与执行差异。
- 可观测性：节点输入/输出/耗时 trace，错误类型，重试次数；模型调用日志与失败率监控。
- 部署规格（MVP 建议）：后端服务 2-3 副本，4C8G 起步；模型网关可独立扩缩容；开启 API 网关鉴权与 TLS；日志集中收集；KMS/Secrets 管理凭据。

## 10. 数据与配置模型（示例）
- 数据源：id, type(mysql|postgres|chroma|milvus), conn_info, auth, status, created_by
- 查询模板：id, name, description, data_source_id, parameters(definition), default_chart, permissions
- 执行日志：id, user_id, source_id, query_text, generated_sql/vector_req, duration, status, error, created_at
- 图表配置：chart_type, encoding(dimensions/measures), interactions, export_options
- 模型注册：id, provider(aliyun|doubao|kimi|openai), model_name, purpose(intent|sql|summary|embedding), endpoint, api_key_ref, qps_limit, daily_quota, cost_metric
- 模型路由策略：id, scenario, primary_model_id, fallback_model_ids, rules(latency/cost/success_rate), health_status
- 模型调用日志：id, request_id, scenario, model_id, latency_ms, tokens_in/out, cost_est, status, error_type, created_at
- 配额使用：model_id, date, calls, tokens_in/out, success_rate

## 11. 验收标准（MVP）
- 功能：自然语言查询、SQL 生成执行、ChromaDB 检索、饼/柱/折线图渲染与切换、数据源配置与连通性测试。
- 可靠性：错误有提示与日志；失败可重试；超时可中断。
- 安全：无裸露凭据；SQL 有 limit；权限校验覆盖配置/查询/导出。
- 体验：关键路径 3 步内完成查询与出图；导出可用。

## 12. 里程碑（示例）
- M1：数据源配置与连通性；MySQL/PostgreSQL 查询链路；基础图表（饼/柱/折线）；审计日志。
- M2：LangGraph 编排；可视化推荐；ChromaDB 向量检索；图表交互（切换/过滤/导出）。
- M3：模型管理与路由（阿里云/豆包/Kimi/OpenAI）；配额/限流/告警；Milvus 接入；混合查询；模板与发布；版本回滚；可观测面板。
- M4：高级图表（漏斗/雷达/桑基）；自动洞察（异常/趋势解读）；权限分级与告警。

## 15. 时序示例（模型路由版）
1) 前端提交：自然语言/参数 + 数据源/模板 ID。
2) 编排：意图解析 → 模型网关路由（选阿里云/豆包/Kimi/OpenAI）→ 数据源选择 → SQL/向量生成。
3) 执行：关系型查询/向量检索 → 结果聚合与补全。
4) 可视化推荐：返回默认图型与可替代图型。
5) 前端渲染，支持切换/过滤/导出；日志与观测记录全链路。

## 13. 依赖与风险
- LLM 质量：需优质模型和提示工程；需错误兜底与重试。
- 数据质量与 schema 变更：需定期同步与缓存；变更告警。
- 向量库性能：需评估索引与硬件资源；批量检索优化。
- 安全合规：数据库最小权限，日志脱敏，防注入。

## 14. 未来扩展
- 更多数据源：ClickHouse、BigQuery、Elastic、Pinecone。
- 多页面仪表板与协作分享；订阅与告警。
- 自动洞察与自然语言解读；结果问答。
- 插件化图表与分析算子市场。

