# DataMind 开发文档

## 项目状态

### ✅ 已完成

#### 后端核心架构
1. **配置管理** (`app/core/config.py`)
   - 基于 Pydantic Settings 的统一配置管理
   - 支持环境变量和 .env 文件
   - 单例模式确保配置一致性

2. **数据模型** (`app/models/`)
   - Pydantic schemas：API 请求/响应模型
   - SQLAlchemy ORM：数据库模型
   - 支持数据源、模型配置、查询日志等核心实体

3. **数据连接器** (`app/connectors/`)
   - 抽象基类 `BaseConnector`：统一接口
   - 工厂模式 `ConnectorFactory`：动态创建连接器
   - 已实现：
     - MySQL 连接器
     - PostgreSQL 连接器
     - ChromaDB 连接器
     - Milvus 连接器
   - 支持连接测试、查询执行、Schema 获取

4. **模型网关** (`app/services/model_gateway.py`)
   - 统一的多模型调用接口
   - 支持阿里云、豆包、Kimi、OpenAI
   - 健康状态监控
   - 调用统计和错误记录
   - 重试机制

5. **编排引擎** (`app/services/orchestrator.py`)
   - 基于 LangGraph 的状态机
   - 节点包括：
     - 意图解析
     - 数据源选择
     - SQL/向量生成
     - 查询执行
     - 可视化推荐
     - 错误处理

6. **API 路由** (`app/api/`)
   - 查询 API：执行查询、获取日志
   - 数据源 API：CRUD、连接测试
   - FastAPI 自动文档生成

7. **工具函数** (`app/utils/`)
   - 加密管理：连接信息加密存储
   - 缓存管理：查询结果缓存

#### 前端核心功能
1. **基础架构**
   - React + TypeScript + Vite
   - Ant Design UI 组件库
   - 路由配置

2. **页面实现**
   - 工作台（Home）
   - 查询工作台（QueryWorkspace）
   - 数据源管理（DataSourceManagement）
   - 模型网关（ModelGateway，占位）
   - 日志与观测（Logs）

3. **服务层**
   - API 客户端封装
   - 查询服务
   - 数据源服务

4. **类型定义**
   - 完整的 TypeScript 类型定义
   - 与后端 Schema 对应

### 🚧 待完善

#### 后端
1. **意图解析**
   - [ ] 集成真实的 LLM 调用
   - [ ] 完善意图解析 prompt
   - [ ] 支持更复杂的查询意图识别

2. **SQL 生成**
   - [ ] 基于 Schema 的 SQL 生成
   - [ ] 支持复杂查询（JOIN、子查询等）
   - [ ] SQL 安全检查和优化

3. **向量检索**
   - [ ] 完整的 embedding 生成
   - [ ] 向量检索结果格式化
   - [ ] 混合查询（关系型 + 向量）

4. **模型网关**
   - [ ] 完整的配额管理
   - [ ] 限流实现
   - [ ] 熔断机制
   - [ ] 路由策略完整实现

5. **可视化推荐**
   - [ ] 基于数据特征的智能推荐
   - [ ] 支持更多图表类型
   - [ ] 图表配置生成

6. **数据源管理**
   - [ ] Schema 同步和缓存
   - [ ] 连接池管理
   - [ ] 健康检查定时任务

7. **认证和权限**
   - [ ] JWT 认证
   - [ ] 角色权限控制
   - [ ] 操作审计

8. **查询模板**
   - [ ] 模板管理 API
   - [ ] 参数化查询支持
   - [ ] 模板版本管理

#### 前端
1. **查询工作台**
   - [ ] 完善图表渲染（ECharts 集成）
   - [ ] 图表类型切换
   - [ ] 数据导出功能
   - [ ] 查询历史记录

2. **数据源管理**
   - [ ] 连接信息加密显示
   - [ ] Schema 预览
   - [ ] 批量操作

3. **模型网关页面**
   - [ ] 模型注册表单
   - [ ] 路由策略配置
   - [ ] 健康状态监控
   - [ ] 调用统计图表

4. **日志页面**
   - [ ] 高级筛选
   - [ ] 日志详情查看
   - [ ] 导出功能

5. **其他**
   - [ ] 错误边界处理
   - [ ] 加载状态优化
   - [ ] 响应式布局完善

## 代码结构说明

### 后端架构设计原则

1. **分层清晰**
   - API 层：处理 HTTP 请求/响应
   - Service 层：业务逻辑
   - Connector 层：数据访问抽象
   - Model 层：数据模型定义

2. **可扩展性**
   - 连接器采用工厂模式，易于添加新数据源
   - 模型网关支持多提供商
   - 编排引擎节点可插拔

3. **可维护性**
   - 统一的错误处理
   - 完善的日志记录
   - 清晰的代码注释

### 前端架构设计原则

1. **组件化**
   - 页面组件独立
   - 可复用组件提取
   - 统一的样式管理

2. **类型安全**
   - 完整的 TypeScript 类型定义
   - API 接口类型对应

3. **状态管理**
   - 使用 Zustand（可选）
   - 服务层封装 API 调用

## 开发指南

### 添加新数据源

1. 在 `app/connectors/` 创建新的连接器类，继承 `BaseConnector`
2. 实现所有抽象方法
3. 在 `app/connectors/registry.py` 中注册

### 添加新模型提供商

1. 在 `app/services/model_gateway.py` 的 `_create_model` 方法中添加新提供商逻辑
2. 更新 `ModelProvider` 枚举

### 添加新的编排节点

1. 在 `app/services/orchestrator.py` 中添加新节点方法
2. 在 `_build_graph` 中注册节点和边

### 添加新 API 端点

1. 在 `app/api/routes/` 创建新的路由文件
2. 在 `app/api/main.py` 中注册路由

## 测试建议

### 单元测试
- 连接器测试
- 服务层测试
- 工具函数测试

### 集成测试
- API 端点测试
- 编排流程测试
- 端到端测试

## 部署建议

### 开发环境
- 使用 SQLite 数据库
- 本地文件存储
- 单进程运行

### 生产环境
- PostgreSQL/MySQL 作为主数据库
- Redis 作为缓存
- 对象存储（如 S3）存储向量库数据
- 容器化部署（Docker）
- 使用 KMS 管理密钥

## 性能优化

1. **查询优化**
   - 查询结果缓存
   - Schema 缓存
   - 连接池优化

2. **模型调用优化**
   - 批量调用
   - 异步处理
   - 结果缓存

3. **前端优化**
   - 代码分割
   - 懒加载
   - 虚拟滚动

## 安全考虑

1. **数据安全**
   - 连接信息加密存储
   - SQL 注入防护
   - 参数化查询

2. **API 安全**
   - JWT 认证
   - 权限验证
   - 请求限流

3. **日志安全**
   - 敏感信息脱敏
   - 审计日志

## 后续计划

1. **MVP 完善**（1-2周）
   - 完成核心功能实现
   - 基础测试和文档

2. **功能增强**（2-4周）
   - 完善模型集成
   - 优化可视化推荐
   - 添加更多图表类型

3. **企业级功能**（4-8周）
   - 权限系统
   - 多租户支持
   - 性能监控
   - 告警系统
