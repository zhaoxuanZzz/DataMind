# DataMind（数据灵析）项目设计方案

## 1. 项目定位
- 智能化数据分析与可视化编排工具，面向多业务场景的查询与洞察。
- Python 后端基于 LangChain + LangGraph 编排多步推理，自动匹配数据源、生成分析计划、执行 SQL/向量检索，并输出可视化图表。
- 前端 TypeScript 提供低代码页面配置，适配多种数据库与图表形态。

## 2. 目标与价值
- **多场景适配**：业务查询、运营看板、实验分析、内容相似度检索等。
- **智能化**：自动理解查询意图，选择合适的数据源、分析方法和可视化类型。
- **可视化洞察**：根据查询目标动态呈现饼图、柱状图、折线图等多种图表。
- **易配置**：页面可视化配置数据源（MySQL、PostgreSQL）、向量库（ChromaDB、Milvus）与图表样式。

## 3. 核心能力
- 查询意图识别与任务分解：利用 LLM 解析自然语言，将需求拆解为检索、计算、可视化步骤。
- 数据源选择与 SQL/向量生成：基于 schema/collection 描述自动生成 SQL 或向量检索请求。
- 数据分析与聚合：支持分组统计、时间序列分析、Top-N、漏斗等常见分析。
- 可视化推荐：结合数据类型与分析意图推荐图表（饼/柱/折/面积/散点/表格）。
- 多数据源融合：支持关系型库 + 向量库的混合查询与结果合并。

## 4. 系统架构概览
- **前端 (TypeScript)**：配置中心、查询编排 UI、可视化渲染、结果联动。
- **后端 (Python + LangChain + LangGraph)**：编排状态机，连接 LLM、SQL/向量工具、缓存与日志。
- **数据源层**：MySQL、PostgreSQL、ChromaDB、Milvus，可按页面配置接入。
- **可视化层**：统一图表渲染协议（数据 + 元数据 + 推荐图型），前端动态渲染。
- **模型网关层**：封装阿里云/豆包/Kimi/OpenAI，多模型路由、限流、熔断与观测。

## 5. 模块设计
- **前端**
  - 数据源管理：表单配置连接信息，测试连通性，保存凭据（加密存储）。
  - 查询面板：自然语言输入、结构化过滤器、参数模板；展示生成的 SQL/向量请求。
  - 图表区：根据推荐或手动选择图表类型；支持交互（筛选、缩放、tooltip、联动）。
  - 配置与发布：页面模板化、权限控制、版本回滚。
- **后端**
  - 编排引擎：LangGraph 构建有向图/状态机，节点包含意图解析、数据源选择、SQL/向量生成、执行、可视化推荐。
  - LLM 与工具接口：LangChain Tools 适配 SQL 生成器、Schema 读取、向量检索器、指标计算器；通过模型网关调用阿里云/豆包/Kimi/OpenAI。
  - 数据连接器：抽象统一接口，支持 MySQL、PostgreSQL；向量库 ChromaDB、Milvus。
  - 结果整理：统一数据帧格式输出，附带字段描述、类型、统计摘要。
  - 日志与可观测性：节点级 trace、输入输出记录、错误重试。
  - 模型网关：统一 API 封装，提供路由策略（成本/延迟/成功率）、健康检查、熔断与限流、配额统计、失败降级。

## 6. 数据流（单次查询示例）
1) 用户输入自然语言需求或选择模板。
2) 意图识别节点解析分析目标、指标/维度、时间范围、期望图表。
3) 数据源选择节点基于 schema/collection 元数据确定库与表/集合。
4) SQL/向量生成节点构造查询；必要时组合多源结果。
5) 执行与计算节点运行查询/检索并做聚合、排序、Top-N、补全时间序列。
6) 可视化推荐节点根据数据类型与意图输出图表配置。
7) 前端渲染图表，并允许用户切换图型或下载数据。

## 7. 数据源与配置
- 页面配置项：连接名称、类型（MySQL/PostgreSQL/ChromaDB/Milvus）、地址/端口、认证方式、数据库/collection、超时、最大行数、向量维度与度量方式。
- Schema 同步：定期拉取并缓存表结构/embedding collection 描述，用于提示与自动生成。
- 凭据存储：服务端加密保存，前端仅取用 token，不回显明文。
- 模型注册：provider(aliyun|doubao|kimi|openai)、model_name、用途(purpose: intent/sql/summary/embedding)、endpoint、api_key_ref、qps_limit、daily_quota、cost_metric。
- 模型路由策略：scenario、primary_model、fallback_list、规则(latency/cost/success_rate)、health_status。

## 8. 可视化设计
- 支持图表：饼图、环形图、柱状图、条形图、折线图、面积图、散点图、表格、组合图。
- 推荐策略：依据指标数量、维度类型（时间/分类/数值）、数据量与对比需求选择默认图型。
- 交互：过滤、联动高亮、区间缩放、导出 PNG/CSV。

## 9. AI 与提示策略
- 提示模板：包含业务描述、可用数据源 schema、示例 SQL/向量查询、输出格式约束。
- 约束执行：对于 SQL 生成启用安全模式（限制 DDL/DML），自动添加 limit 与超时。
- 反馈循环：执行失败时返回错误信息给 LLM 节点重试或调整查询。

## 10. 非功能性要求
- 安全：最小权限数据库账号、参数化查询、防止提示注入、请求审计。
- 性能：缓存热门 schema 与查询结果，向量检索批量化；并行执行可独立的子查询。
- 可用性：节点级重试与熔断；明确超时策略；前端错误提示友好。
- 可扩展：新增数据源与图表类型需遵循统一接口。

## 11. MVP 与迭代
- MVP：单页面查询与可视化，支持 MySQL/PostgreSQL + ChromaDB；饼/柱/折线图；基础权限与日志。
- 迭代方向：Milvus 支持、更多图表（漏斗/雷达/桑基）、多页面仪表板、协作与分享、自动洞察（异常检测/趋势解读）、企业级权限与审计。
- 模型管理扩展：接入阿里云/豆包/Kimi/OpenAI，提供路由与配额；后续支持更多模型供应商。

## 12. 里程碑（示例）
- M1：完成数据源配置、基础查询链路、SQL 安全执行、基础图表渲染。
- M2：引入 LangGraph 状态机、可视化推荐、ChromaDB 向量检索、多图表切换。
- M3：模型网关上线（阿里云/豆包/Kimi/OpenAI 路由）、配额与限流；Milvus 接入、混合查询、仪表板模板、可观测性完善。
- M4：自动洞察与解释、权限分级、团队协作与分享。

## 13. 部署与运维建议
- 组件拆分：前端（静态资源/SSR 可选）；后端服务（编排 + 模型网关）；数据连接器适配层；向量/关系数据库外部托管。
- 规模建议（MVP）：后端 2-3 副本，4C8G 起步；模型网关可独立实例以便弹性扩容；缓存/队列（可选，用于 schema 缓存与异步任务）。
- 安全：API 网关鉴权，模型凭据与数据库凭据 KMS/Secrets 管理；审计日志集中存储。
- 观测：链路 trace、模型调用日志、查询耗时面板；错误告警（失败率、超时、熔断触发）。

## 14. 时序（简要示例）
1) 前端提交查询：携带自然语言/参数、数据源/模板 ID。
2) 编排：意图解析 → 模型网关路由（选阿里云/豆包/Kimi/OpenAI）→ 数据源选择 → SQL/向量生成。
3) 执行：关系型查询/向量检索 → 结果聚合/补全。
4) 可视化推荐：根据数据类型返回图表配置。
5) 前端渲染并提供交互；后端记录日志与指标。

